[project]
name = "comfyui-ltxlongaudio"
version = "0.2.0"
description = "Custom nodes and compatibility shims for long-audio LTX workflows in ComfyUI"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "GPL-3.0-or-later" }

[tool.pytest.ini_options]
testpaths = ["tests"]
